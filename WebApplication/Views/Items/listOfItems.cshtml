
    <h1> Все товары </h1>
    <h3> @Model.currentCategory </h3>
    @{ 
        foreach (var item in Model.allItems)
            {
                <div>
                    <h2> Модель: @item.name </h2>
                    <p> Цена: @item.price.ToString ("c") </p>
                </div>
            }
    }
